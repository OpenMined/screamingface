apiVersion: v1
kind: Service
metadata:
  name: {{ include "aigateway-ui.fullname" . }}
  labels:
    {{- include "aigateway-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "aigateway-ui.selectorLabels" . | nindent 4 }}
