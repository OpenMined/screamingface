url4-cloud {{ .Chart.AppVersion }} deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

The App (stateless REST + WebSocket control plane) is running as:
  Deployment/{{ include "url4-cloud.fullname" . }}
  Service/{{ include "url4-cloud.fullname" . }} (port {{ .Values.service.port }})

RBAC (namespace-scoped): the App runs Runner Jobs via ServiceAccount
"{{ include "url4-cloud.serviceAccountName" . }}", bound to Role/{{ include "url4-cloud.fullname" . }}
(batch/jobs: create,get,list,watch,delete · pods: get,list · pods/log: get).

{{- if .Values.ingress.enabled }}

Reachable via Ingress:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}

Port-forward to reach the App locally:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "url4-cloud.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- end }}

NATS bus: {{ .Values.config.natsUrl }}
{{- if not .Values.nats.enabled }} (external — reusing an existing in-cluster NATS with JetStream){{- end }}
