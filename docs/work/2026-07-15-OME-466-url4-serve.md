---
ticket: OME-466
stack: url4
status: in_progress   # planned | in_progress | done | blocked
started: 2026-07-15
finished:   # closed 2026-07-15; reopened 2026-07-16 (commands-only scope extension), re-closed 2026-07-16; reopened 2026-07-17 (PR #402 review round 2)
---

# OME-466 — url4 serve: minimal CLI to run the engine as an HTTP node

## Intent

> **SUPERSEDED (2026-07-16) — historical.** This was the intent at work START, before both
> pivots. The aigateway connector, the `url4[serve]` extra name, and the Starlette/uvicorn
> quarantine below are all obsolete: the extra is **`url4[server]`** (uvicorn only, Starlette
> never added), and `url4 serve` ships **no aigateway connector** — `[commands]` is the only
> backend kind. Current intent: the "Scope extension (2026-07-16)" section.
> *This ledger is a chronological record — superseded sections are marked, not rewritten.*

Expose the framework-free `url4` engine as a runnable **url4 node** via a minimal CLI +
HTTP server, shipped as an optional `url4[serve]` extra on `packages/url4`. A user runs
`url4 serve` to stand up an endpoint that evaluates url4 expressions — fanning out model
calls to the aigateway (`:9105`) and dispatching custom/command routes locally — realizing
the doctrine's "url4 expression IS the address" node model (N1/N4/N5) at the SDK layer.
Companion `url4 eval` gives a network-free one-shot. Keeps the core import graph
framework-free (Starlette/uvicorn quarantined behind the extra, like `io_http` quarantines
httpx).

## Planned changes (REVISED — built on existing `Url4Node`; see spec/plan pivot)

- `packages/url4/pyproject.toml` — `[project.scripts] url4 = "url4.cli:main"`. Reuses the
  existing `[project.optional-dependencies] server = ["uvicorn"]` extra. **No Starlette.**
- `packages/url4/src/url4/_serve.py` — `ServeConfig` + `resolve()`/`validate()` (flag > env >
  url4.toml > default); `DEFAULT_ROUTES`; `make_llm_handler` (aigateway `/v1/chat/completions`),
  `make_command_handler` (subprocess); `build_node(config, client)` wiring `@node.endpoint`
  handlers + `data("/healthz","ok")` onto a `Url4Node`; `build_asgi_app(...)` raw-ASGI wrapper
  adding 503 backpressure / 504 timeout / graceful client+node shutdown.
- `packages/url4/src/url4/cli.py` — argparse CLI: `serve` / `eval` / `--version`; stdout/stderr
  discipline; exit codes 0/1/2; lazy uvicorn seam (`_serve_forever`).
- `packages/url4/tests/unit/{test_serve_config,test_serve_backends,test_serve_app,test_cli}.py`
  — keep `--cov=url4` ≥ 95%.
- `docs/spec/…`, `docs/plan/…`, `docs/tasks/…` (created; pivot noted).

**Superseded plan:** the original `url4/server/` subpackage + `GatewayIOLayer` + Starlette app
(`url4/server.py` already exists as the `Url4Node` SDK; a Starlette layer would duplicate it).

## Test plan

- Config resolution precedence (flag > env > toml > default); unknown `--processor` route → fail-fast; bad `--concurrency` rejected.
- IOLayer dispatch: absolute URL → plain GET; bare `/path` → plain GET; `/claude?q=(ctx)!intent` → aigateway POST body/model/auth asserted (fake transport); command route → subprocess stdout; unknown route → ResolutionError.
- prompt merge `merge(intent, ctx)` matches the engine's `default_process` convention.
- App: GET/POST eval happy path; `Url4Error` → 400/422/502/504 mapping; over `max_inflight` → 503 + Retry-After; `/healthz` no-run; graceful shutdown closes the client.
- CLI: `eval` from arg + stdin (StaticIOLayer); `--version`; usage error → exit 2; serve wiring smoke (bind + immediate shutdown).

## Acceptance

- See OME-466 acceptance checklist. Core imports with no Starlette/uvicorn installed; all gates green; coverage ≥ 95%.

## Outcome

> **SUPERSEDED (2026-07-16) — historical.** Accurate as of commit `6b2a17f` (pivot 1) and
> obsolete from `867e57f` (pivot 2) onward. In particular: `DEFAULT_ROUTES` /
> `make_llm_handler` / `build_client` were **deleted**; "`url4/__init__.py` and
> `url4/server.py` untouched" is **now false** (both are modified, along with `client.py`,
> `dag/*`, `io_layer.py`, `io_static.py`); and the gate line ("ALL GREEN", "753 passed",
> "Append-only check ✓") describes the pivot-1 run — current is
> `--skip-append-only` / **760 passed** / 97%.
> **AIDEV-NOTE: this block is the source PR #402's description was copied from — do not
> copy from it. Regenerate the PR body from the code at head.** Current state: the "Scope
> extension (2026-07-16)" and "Review round 2 (2026-07-17)" sections.

- **Actual files (as built):**
  - `packages/url4/pyproject.toml` — added `[project.scripts] url4 = "url4.cli:main"` (reused the
    existing `[server]` uvicorn extra; no Starlette added).
  - `packages/url4/src/url4/_serve.py` (426 lines) — `ServeConfig`/`resolve`/`validate`,
    `DEFAULT_ROUTES`, `make_llm_handler`, `make_command_handler`, `build_client`, `build_node`,
    `build_asgi_app` (503/504/graceful-shutdown wrapper).
  - `packages/url4/src/url4/cli.py` (175 lines) — argparse `serve`/`eval`/`--version`; lazy
    `_serve`/uvicorn seams; stdout/stderr discipline; exit codes 0/1/2.
  - Tests: `tests/unit/{test_serve_config,test_serve_backends,test_serve_app,test_cli}.py`
    (49 tests). `url4/__init__.py` and `url4/server.py` untouched.
  - Docs: spec/plan/tasks/work (this file); spec+plan carry the Url4Node pivot note.
- **Gates:** `run_gates.py url4` — ALL GREEN (ruff check, ruff format --check, pyright,
  pytest --cov=url4 --cov-fail-under=95). 753 passed (704 baseline + 49 new); new modules
  `_serve.py`/`cli.py` at **100%** line coverage. Append-only check ✓ (no prior test touched).
- **Verification (beyond tests):** ran the real console script — `url4 --version`, `url4 eval`
  (arg + stdin), `url4 serve` without the extra (hint + exit 2), and a live uvicorn serve +
  curl: `/healthz`→200, `GET /v1?q=('cats','dogs')!'Compare'`→correct merge, parse error→400,
  `POST /v1`→405 (GET-only). Confirmed `import url4` pulls neither uvicorn nor starlette.
- **Deviations:**
  1. **Owner-approved design pivot** — built on the existing `url4.Url4Node` (endpoint registry
     + raw-ASGI `asgi()` + `serve()`), NOT a from-scratch Starlette app + `GatewayIOLayer`. No
     Starlette dependency. HTTP surface is `GET {eval_path}?q=` (default `/v1`), GET-only per
     doctrine N1 (POST escape hatch dropped); `/healthz` is a `data()` route; the node owns the
     `Url4Error→HTTP` mapping and the CLI wrapper adds only 503/504/shutdown. Default port 4404
     (SDK default). Spec/plan updated with the pivot; original Starlette plan superseded.
  2. **Linear label drift** — the card's Epic/workstream group (e.g. "url4 Engine") has been
     removed from Linear; OME-466 filed with `pkg/url4-python-sdk` + `autonomous` + `agentic`
     only. Card taxonomy needs an owner reconcile.
  3. Filed via `linear-cli` (owner-approved) since the Linear MCP was inactive this session;
     card still says MCP-only and should be updated.

## Post-review follow-up (2026-07-15) — commit `0a211fe`

Ran `/qa-tester` + `/simplify` + `/code-review` over the branch. Code was structurally
clean (no simplifications applied — apparent duplication vs `server.py` is justified by
divergent behavior); review surfaced two latent edge-case bugs, both fixed with regression
tests on the same branch/PR:

- **eval-path/`/healthz` clash** — `ServeConfig.validate()` guarded routes/commands against
  the reserved paths but not `eval_path == /healthz`; `url4 serve --eval-path /healthz`
  collided at `build_node` time with an uncaught `ValueError` (traceback, exit 1) instead of
  a fail-fast `ConfigError` (exit 2). Added a `validate()` guard.
- **non-UTF-8 command output** — `_run_command` decoded subprocess stdout/stderr strictly;
  a command exiting 0 with non-UTF-8 output raised a raw `UnicodeDecodeError`, bypassing the
  `Url4Error → HTTP` mapping (bare 500). Now decodes with `errors="replace"`.

Gates re-run: ruff/format/pyright clean; `_serve.py`/`cli.py` still **100%** coverage;
full package suite **755 passed** (753 + 2 regression tests). Pushed to the PR branch.

## Scope extension (2026-07-16) — commands-only serve + `default_route` (owner-directed)

### Intent

Owner decision on the same branch/PR: `url4 serve` stops shipping an aigateway
connector. Routes and commands are the same thing at the SDK core (an endpoint), so
the serve layer keeps ONE concept: `[commands]` — user-owned subprocess argv
templates (a user who wants an LLM backend writes their own gateway script and
mounts it as a command). The serve-layer `processor` config is renamed
`default_route`; it must name a declared command route (ConfigError otherwise) and
defaults to the FIRST declared command route. Empty `[commands]` → fail-fast
ConfigError. Core SDK keeps the `processor` NAME but loses the hardcoded
`"/claude"` default: unset means "first registered endpoint" on `Url4Node` and
"no reduce route — clear ResolutionError on fan-out reduce" on the bare engine.

### Planned changes

- `src/url4/_serve.py` — DELETE the aigateway connector: `DEFAULT_ROUTES`,
  `make_llm_handler`, `_extract_content`, `_merge`, `build_client`,
  `_resolve_token`, `_flag_routes`, `_CHAT_PATH`, the `httpx` import, and the
  `backend_url`/`backend_token`/`routes` config fields. `ServeConfig.processor` →
  `default_route: str | None = None` + `resolved_default_route` property
  (explicit value, else first command). `validate()`: commands non-empty;
  explicit `default_route` ∈ commands; reserved-path checks now against commands
  only. `build_node(config)` / `build_asgi_app(node, config)` lose the client
  param; lifespan closes the node only.
- `src/url4/cli.py` — drop `--backend-url`/`--backend-token`/`--route`;
  `--processor` → `--default-route` (env `URL4_DEFAULT_ROUTE`, toml
  `default_route`); `_SERVE_FIELDS` updated.
- Core (name kept, selection behavior changed): `dag/node.py` — remove
  `DEFAULT_PROCESSOR`, `ExecutionContext.processor: str | None = None`;
  `dag/executor.py` — `run(processor: str | None = None)`, ctx-clash guard on
  `is not None`; `dag/nodes.py` — `FanoutReduceNode.resolve` raises
  `ResolutionError` when `ctx.processor is None`; `client.py` —
  `Client(processor: str | None = None)`; `server.py` — `Url4Node(
  default_processor: str | None = None)` resolves unset → first registered
  endpoint at eval time, explicit-but-unregistered → `ResolutionError`.
- Tests: rewrite `tests/unit/{test_serve_config,test_serve_backends,
  test_serve_app,test_cli}.py` for the commands-only world (owner-approved
  append-only exception); append core tests (`test_dag.py`, `test_server.py`,
  `test_client.py`); update any prior core test that pinned the implicit
  `"/claude"` default (mandated by the owner's core-behavior decision — each
  one listed in the Outcome).
- Docs: revision notes in `docs/spec/2026-07-15-url4-serve-spec.md` +
  `docs/plan/2026-07-15-url4-serve.md`; `docs/tasks/2026-07-15-url4-serve.md`
  mirror description; Linear `OME-466` comment (linear CLI — MCP inactive,
  owner-approved precedent).

### Test plan

- Config: empty `[commands]` → ConfigError; explicit `default_route` not in
  commands → ConfigError; unset `default_route` resolves to first declared
  command (TOML order); precedence flag > env > toml for `default_route`;
  removed flags/keys are gone (`--route`, `--backend-url`, `--backend-token`,
  `[routes]` ignored); command path/argv validation and reserved-path clashes
  still enforced.
- Backends: command handler behavior unchanged (stdin piping, `{intent}`/
  `{context}` substitution, timeout, non-zero exit, non-UTF-8); LLM handler
  gone from the module surface.
- App/node: `build_node(config)` registers commands only + `/healthz`;
  reduce dispatches to `resolved_default_route`; lifespan shutdown closes the
  node (no shared client).
- CLI: `--default-route` wired; removed flags rejected by argparse; empty-
  commands serve exits 2 with the fail-fast message.
- Core: `run()` fan-out reduce with no processor → ResolutionError naming the
  fix; `Url4Node` with no `default_processor` reduces via the FIRST registered
  endpoint; explicit unregistered `default_processor` → ResolutionError;
  `ctx`+kwargs clash guard still fires with `processor="/x"`.

### Acceptance

- `url4 serve` runs with a `[commands]`-only url4.toml and no aigateway
  anywhere; zero-config serve fails fast with an actionable message; core
  imports/gates green; coverage ≥ 95%; append-only exceptions limited to the
  owner-approved list above.

### Outcome (scope extension)

- **Actual files:**
  - Serve layer: `_serve.py` (437 → ~330 lines; connector deleted: `DEFAULT_ROUTES`,
    `make_llm_handler`, `_extract_content`, `_merge`, `build_client`, `_resolve_token`,
    `_flag_routes`, httpx import, `backend_url`/`backend_token`/`routes`; added
    `default_route` field + `resolved_default_route` property + commands-required
    validation); `cli.py` (`--default-route`; `--route`/`--backend-url`/
    `--backend-token` removed; no client wiring).
  - Core: `io_layer.py` (+`SupportsDefaultRoute` capability protocol);
    `io_static.py` (+`StaticIOLayer.default_route()` = first routes key);
    `dag/node.py` (`DEFAULT_PROCESSOR` removed; `ExecutionContext.processor:
    str | None = None`, resolution via `_declared_default_route`);
    `dag/executor.py` (`run(processor: str | None = None)`, ctx-clash guard on
    `is not None`); `dag/nodes.py` (`FanoutReduceNode` raises clear
    `ResolutionError` on `processor is None`); `client.py`
    (`Client(processor: str | None = None)`); `server.py`
    (`Url4Node(default_processor: str | None = None)` + public `default_route()`
    = explicit-or-first-endpoint); `__init__.py`/`dag/__init__.py` export updates.
  - Tests: serve suites rewritten (`test_serve_config/backends/app/cli`); core
    additions appended (`test_io_layer`, `test_dag`, `test_ensemble`,
    `test_server`); `tests/conftest.py` RecordingIOLayer delegates
    `default_route`.
  - Docs: spec §11 revision + plan revision note; tasks mirror updated.
- **Prior tests modified (owner-approved exception, AskUserQuestion 2026-07-16):**
  serve suites rewritten wholesale (feature removal); 6 core tests gained an
  explicit `processor="/claude"` (the core no longer defaults to it):
  `test_ensemble.py::test_fanout_reduce_builds_weighted_reducer_input`,
  `test_characterization.py::{test_single_relative_expression_group_takes_fanout_path,
  test_parenthesized_single_relexpr_still_reduces}`,
  `test_dag.py::test_fanout_call_unwraps_guarded_relative_expression`,
  `test_iteration.py::{test_iteration_top_level_intent_reduces_each_row,
  test_directive_after_top_level_intent_is_parsed_not_swallowed}` (+1 stale
  comment in its `_resolver`). Assertions preserved verbatim in all 6.
- **Gates:** `run_gates.py url4 --skip-append-only` — ruff check ✓, ruff format ✓,
  pyright ✓, pytest --cov=url4 --cov-fail-under=95 ✓ (760 passed; total 97%).
  The append-only gate red is exactly the owner-approved list above.
- **Verification (beyond tests):** live `uv run --extra server url4 serve` with a
  commands-only url4.toml — `/healthz`→ok; `GET /v1?q=(/upper(hello world)!'go')`
  → `HELLO WORLD` (subprocess dispatch); ensemble
  `(/upper(a)!'go', /upper(b)!'go')!'choose the best'` → reduce dispatched to the
  FIRST declared command (`REDUCED::[Response 1]…`); parse error→400, unknown
  route→404; `url4 serve` with no commands → exit 2 + actionable message;
  `--default-route /absent` → exit 2 naming the declared commands.
- **Deviations:**
  1. The live-verify `uv run --extra server` transiently installed uvicorn into
     the dev venv, hanging `test_serve_forever_without_uvicorn_prints_hint`
     (real `uvicorn.run`); restored with `uv sync` and hardened
     `test_serve_without_commands_is_usage_error` with cwd isolation.
  2. Linear MCP inactive AND the `linear` CLI unauthenticated at implementation
     time — the OME-466 comment + In Review state were posted later the same
     session once the owner supplied the API key (comment `87fb3332`).

## Review round 2 (2026-07-17) — PR #402 review fixes (owner-directed)

### Intent

Serguey's 2026-07-17 review of PR #402 raised five points. All five were verified
against branch head and all five hold. Four are **documentation drift**, with one
systemic root cause: each pivot appended a revision banner and left the document
body describing the deleted architecture, so the PR body (copied from this
ledger's first Outcome) still advertised the aigateway connector. One is a **real
defect**: an empty host binds every interface with command routes reachable and
both exposure warnings suppressed.

Investigation widened the defect beyond the review's scope. `_pick` accepts an
empty env var as a real value for every *string* field, while int fields already
reject it — so `URL4_HOST=`, `URL4_EVAL_PATH=` and `URL4_DEFAULT_ROUTE=` all
silently adopt `""`, and `eval_path` is never validated for a leading `/` at all
(`eval_path="v1"` and `""` both pass `validate()` today). The empty host is one
symptom of that family; fixing only `_LOOPBACK` would leave the rest.

Verified against head (evidence for the reply to the reviewer):
- The gate claim: the ledger's `--skip-append-only` / 760 is current; the PR
  body's "ALL GREEN / append-only ✓ / 753" is the pre-pivot run. Clean-venv run
  and the PR's own coverage bot at `894b0f3` (97%) both confirm **760**.
- The append-only exception is substantively sound and mechanically checkable:
  across the four core test files, **0 assert lines and 0 test functions were
  removed** — every removal is a `run(...)` call site that gained
  `processor="/claude"`.
- `DEFAULT_PROCESSOR` removal is deliberate, but the release is silently broken
  for an unrelated reason: the repo squash-merges with
  `squash_title: COMMIT_OR_PR_TITLE`, and the PR title `[WIP] feat(url4): …` is
  not a parseable conventional commit, so release-please would **skip url4
  entirely** — no bump, no changelog, `!` lost.

### Planned changes

**A. Docs — the bodies, not more banners** (a spec states the current target, so
its body is rewritten; this ledger is a chronological record, so its superseded
sections are *marked*, never rewritten):
- `docs/spec/2026-07-15-url4-serve-spec.md` — rewrite the stale body: §2
  packaging snippet (`serve = ["starlette…"]` → `server = ["uvicorn>=0.30"]`;
  `url4.server.cli:main` → `url4.cli:main`; drop `src/url4/server/`), §3.1 flag
  table (drop `--backend-url`/`--backend-token`/`--route`, `--processor` →
  `--default-route`, port `4444` → `4404`), §4 (raw-ASGI over `Url4Node`,
  GET-only at `{eval_path}`, no `POST /`), §5 (node endpoints, no
  `url4.server.io_gateway`/`GatewayIOLayer`), §7 (+ the empty-host rule), and
  **§8 acceptance criteria** — the definition of done, currently 8 unticked boxes
  of which 4 describe deleted features.
- `docs/plan/2026-07-15-url4-serve.md` — rewrite the signatures section
  (`class GatewayIOLayer`, `build_app(...) -> Starlette`, `url4.server.app`,
  `backend_url`, `processor: str = "/claude"`, `routes`/`DEFAULT_ROUTES`).
- `docs/tasks/2026-07-15-url4-serve.md` — rewrite the mirror body (doubly stale:
  its scope-extension note fixed the aigateway half, leaving Starlette / `POST /`
  / `GET /?q=` / `url4[serve]` from pivot 1).
- This ledger — mark the superseded Intent + first Outcome; add this section.
- `packages/url4/QUICKSTART.md` — currently **untracked**: the only user-facing
  doc for the new console script exists solely in the working tree. Commit it.

**B. Code — the config-validation family** (`packages/url4/src/url4/`):
- `_serve.py` `_pick()` — an empty env var means UNSET (fall through to toml >
  default), making string fields consistent with the int fields that already
  reject `""`. Fixes the realistic vector: an unresolved interpolation in a
  `.env`/compose file.
- `_serve.py` `validate()` — reject an empty host (covers the explicit
  `--host ""`, which `_pick` normalization cannot catch); require `eval_path` to
  start with `/`, reusing the existing `_require_paths` helper for one message
  format.
- `cli.py` `_LOOPBACK` — drop `""`. Defense in depth: `validate()` now rejects it
  first, but a set named `_LOOPBACK` must not claim that "bind every interface"
  is loopback.

**C. Release metadata / PR** (no reply to the reviewer — owner will send it):
- PR title → `feat(url4)!: …` (drop `[WIP]`) so release-please parses the squash.
- PR body regenerated **from the code at head**, not from any document.
- A `BREAKING CHANGE:` footer for the squash body — neither `867e57f` nor the PR
  has one, so even with `!` the changelog would announce a break without saying
  what broke. Migration note: the reduce default changed from `/claude` to the
  first declared route. In-repo proof it is not theoretical —
  `test_iteration.py`'s own new comment: *"the resolver declares /solve first, so
  relying on the first-declared-route default would mis-dispatch"*.

**Out of scope — separate follow-up tickets, none blocking this merge:**
1. `test_server.py::test_serve_requires_uvicorn` (released v1 code) and its new
   sibling `test_cli.py::test_serve_forever_without_uvicorn_prints_hint` both
   fail if the `[server]` extra is present in the dev venv — the latter calls the
   real `uvicorn.run()` and **binds port 4404**. Both are prior tests; rule 5
   forbids editing them in this unit.
2. `packages/url4` has no README and no `readme` field in `pyproject.toml` — a
   published PyPI package with no long description.
3. Linear label taxonomy reconcile (the card's Epic/workstream group is gone) and
   the card's "Linear via MCP only" rule vs. the owner-supplied-API-key precedent
   now used twice.

### Test plan (RED first; all APPEND-only — no prior test covers these paths)

- `test_serve_config.py`: `URL4_HOST=""`/`URL4_PORT=""`/`URL4_EVAL_PATH=""`/
  `URL4_DEFAULT_ROUTE=""` each fall through to the default; an empty env var
  falls through to **toml** when toml declares the key (proves fall-through, not
  a short-circuit to default); `validate()` rejects an empty host; `validate()`
  rejects a relative (`"v1"`) and an empty (`""`) eval path.
- `test_cli.py`: `serve --host ""` exits 2 (the flag path `_pick` cannot fix);
  `_warn_exposure` with `host=""` emits BOTH warnings (guards the `_LOOPBACK`
  fix against a future removal of the `validate()` guard).

### Acceptance

- `URL4_HOST=` in a compose/.env file can no longer bind `0.0.0.0`/`::`; an
  explicit `--host ""` exits 2; a non-loopback bind still warns twice.
- `eval_path` is validated; the reserved-path and health-path guards still fire.
- Spec §8 describes the shipped system and every box is ticked or justified.
- No document on the branch contradicts the code at head.
- Gates green (`run_gates.py url4`), coverage ≥ 95%, and the append-only gate is
  **clean** for this unit — every test added, none modified.

### Outcome (review round 2)

- **Actual files:**
  - `docs/spec/2026-07-15-url4-serve-spec.md` — body rewritten against head; the two
    revision banners collapsed into a revision-history table + an AIDEV-NOTE naming the
    banner-over-stale-body pattern as the root cause. §8 acceptance rewritten.
  - `docs/plan/2026-07-15-url4-serve.md` — signatures rewritten to AS-BUILT (they had
    survived both pivots unchanged, still specifying `GatewayIOLayer` and
    `build_app(...) -> Starlette`); test-doubles section corrected (no Starlette
    `TestClient`, no httpx `MockTransport` — neither is a dependency).
  - `docs/tasks/2026-07-15-url4-serve.md` — mirror body rewritten (it was stale to BOTH
    pivots; its scope-extension note had only fixed the aigateway half).
  - `packages/url4/src/url4/_serve.py` — `_pick` treats an empty env var as unset;
    `validate()` rejects an empty host and requires `eval_path` to start with `/`
    (reusing `_require_paths`).
  - `packages/url4/src/url4/cli.py` — `""` dropped from `_LOOPBACK` + an INVARIANT anchor.
  - `packages/url4/tests/unit/test_serve_hardening.py` — **new** (7 tests).
  - `packages/url4/QUICKSTART.md` — **committed** (was untracked: the only user-facing doc
    for the console script existed solely in the working tree) + a "Binding & exposure"
    section.
  - Linear OME-466 — description rewritten (see Deviation 3).
- **Commits:** `67e7df9` (spec + ledger), `c675810` (code + tests), this docs commit.
- **Gates:** `run_gates.py url4` — **ALL GATES GREEN**, including
  `✓ append-only test check (vs HEAD)` with **no `--skip-append-only`**. 767 passed
  (760 + 7); total 97%; `_serve.py` and `cli.py` at **100%**.
- **Verification (beyond tests):** re-ran the probe that originally demonstrated the bug.
  Before: `URL4_HOST=""` → `host=''`, `validate()` passed, both warnings skipped, and
  `asyncio` bound `[('0.0.0.0', …), ('::', …)]`. After: `URL4_HOST=""` → `127.0.0.1`;
  `--host ""` → `ConfigError`; `"" in _LOOPBACK` → `False`; `URL4_PORT=""` → `4404`;
  `eval_path="v1"` → `ConfigError`. Regression: `0.0.0.0` still validates and still warns.
- **Deviations:**
  1. **New test module instead of appending** to `test_serve_config.py`/`test_cli.py`.
     The append-only gate compares `git diff --name-status HEAD` at **file** level, so it
     cannot distinguish an append from an edit — appending would have turned the gate red
     and forced another `--skip-append-only` run, recreating exactly the ambiguity this
     review round raised ("which gate run is accurate?"). A new file keeps "were prior
     tests touched?" answerable from the gate output alone. Rule 5 explicitly blesses
     adding tests. Rationale recorded in the module docstring.
  2. **Two guards, not one.** The review proposed dropping `""` from `_LOOPBACK` and
     rejecting it in `validate()`. That covers the flag vector but not the realistic one:
     an unresolved `.env`/compose interpolation. `_pick` normalization handles the env
     vector for every field uniformly (and fixed `URL4_PORT=""`, which previously errored
     rather than defaulting — strings and ints now agree); `validate()` handles the
     explicit `--host ""`. The `eval_path` gap was found in the same family and fixed here.
  3. **Linear OME-466 was a FIFTH stale artifact.** Not visible in earlier rounds (no API
     key, MCP inactive); once the owner supplied the key its description was still fully
     pre-pivot — Starlette, `POST /`, aigateway `:9105`, `url4[serve]`,
     `url4.server.cli:main`, "command routes off by default". Rewritten against head.
     Status was `In Review` there and in the mirror, so this ledger's `done` was the
     outlier of the three.
  4. **Out of scope, filed as follow-ups** (see Planned changes): the uvicorn-dependent
     test fragility (both are prior tests — rule 5), the missing README/`readme` field,
     and the Linear label + "MCP-only" card reconciles.
  5. **No reply posted to the reviewer** — owner-directed; the evidence is assembled here
     and in the PR body for the owner to send.
