---
ticket: OME-400
stack: screamingface
status: complete
started: 2026-07-18
finished: 2026-07-18
---

# OME-400 — Phase 1 engine-profile walkthrough notebook

## Intent

Add an executable development walkthrough for the implemented Phase 1 SDK and temporary
sf-url4-engine profile. It must teach setup, discovery, remote benchmark loading, local value
authoring, and Fusion authoring without implying that Phase 2 execution already exists.

## Planned changes

- Add a notebook generator under `packages/screamingface/scripts/`.
- Generate `packages/screamingface/examples/phase_1_engine_profile.ipynb` from that source.
- Explain Docker and Hugging Face prerequisites, the default engine URL, registry plaintext,
  discovery filters, eager benchmark loading, local benchmark construction, and immutable Fusion
  authoring.
- Keep remote dataset loading explicit and opt-in so the remaining walkthrough runs without a
  Hugging Face login.
- State the Phase 1 boundary clearly: no model calls, `run`, or `evaluate` yet.

## Test plan

- Build the notebook from its generator and confirm a clean regeneration diff.
- Execute the notebook from a clean kernel against the Docker engine registry.
- Run Ruff, formatting, Pyright, and the ScreamingFace test suite.

## Outcome

- **Actual files:** added `scripts/build_phase1_engine_profile.py` and its generated
  `examples/phase_1_engine_profile.ipynb`; replaced the stale package README with the current
  Phase 1 setup, boundary, and walkthrough link; added JupyterLab to the notebook extra; added a
  deterministic notebook-regeneration CI step; and reconciled the OME-400 task and plan.
- **Commits:** pending owner commit.
- **Gates:** the 21-cell notebook regenerated byte-for-byte, executed from a clean kernel against
  the real tracked engine image with no cell errors, and rendered to HTML; Ruff, notebook format,
  Pyright, 66 SDK tests at 97% coverage, 9 engine-app tests at 100% coverage, package sdist/wheel,
  and `git diff --check` are green.
- **Deviations:** remote `benchmarks.load(...)` remains an explicit opt-in notebook cell because
  it requires real Hugging Face access. The rest of Run All uses the real HTTP engine and no mock.
  The existing owner-run hidden-spike containers on ports 4404/9105 were left untouched; clean
  execution used the tracked app image on port 4414 through `SCREAMINGFACE_ENGINE_URL`.
