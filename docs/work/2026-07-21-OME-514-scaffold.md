---
ticket: OME-514
stack: url4-cloud
status: in_progress
started: 2026-07-21
finished:
---

# OME-514 — scaffold apps/url4-cloud

## Intent
Stand up `apps/url4-cloud` (Python · uv · FastAPI) with the four src packages, a two-entrypoint
`pyproject`, ruff+pyright (with `C901`/PLR), a `url4-cloud` stack entry in the card, CI lane,
CODEOWNERS, and dependabot — the foundation every later url4-cloud unit imports. Spec §10.

## Planned changes
- `apps/url4-cloud/pyproject.toml` (uv/hatchling; entrypoints `url4-cloud`, `url4-cloud-runner`).
- `apps/url4-cloud/src/{url4_cloud,url4_cloud_runner,url4_cloud_nats,url4_cloud_protocol}/`.
- `apps/url4-cloud/src/url4_cloud/{app.py,config.py,cli.py}` (create_app + /healthz).
- `apps/url4-cloud/tests/unit/test_app.py` (RED first: /healthz → 200).
- `.claude/sdlc.local.md` (url4-cloud stack entry + gates).
- `.github/workflows/url4-cloud-tests.yml`, `.github/CODEOWNERS`, `.github/dependabot.yml`.

## Test plan
- RED: `test_healthz_ok` fails (no app) → GREEN with a minimal `create_app`.
- Gates: `run_gates.py url4-cloud` green (ruff · ruff format · pyright · pytest+cov).

## Acceptance
- `uv run` app boots; `/healthz` 200; all card gates green; stack entry present.

## Outcome (fill at the end — required before COMMIT)
- **Actual files:** `apps/url4-cloud/pyproject.toml`, `README.md`; `src/url4_cloud/{__init__,config,app,cli}.py`;
  `src/url4_cloud_runner/{__init__,__main__}.py`; `src/url4_cloud_nats/__init__.py`;
  `src/url4_cloud_protocol/__init__.py`; `tests/unit/test_app.py`; `.claude/sdlc.local.md` (stack entry);
  `.github/workflows/url4-cloud-tests.yml`; `.github/CODEOWNERS`; `.github/dependabot.yml`.
- **Commits:** see the OME-514 commit on `OME-513-url4-cloud`.
- **Gates:** `run_gates.py url4-cloud` ALL GREEN — append-only ✓ · ruff ✓ · ruff format ✓ · pyright ✓ ·
  pytest+cov ✓.
- **Deviations:** healthz scaffold test written alongside the app (strict RED-first begins with the
  OME-515 protocol invariants); coverage floor 80% (app) vs url4's 95%; process entrypoints
  (`cli.main`, runner `main`) `# pragma: no cover`; CODEOWNERS → `@sergio-bershadsky` (url4 domain).
