---
ticket: OME-515
stack: url4-cloud
status: done
started: 2026-07-21
finished: 2026-07-21
---

# OME-515 — url4_cloud_protocol frame models + taxonomy invariants

## Intent
The shared Pydantic v2 frame contract (spec §7/§7.1/§8): envelope header, taxonomy (Decimal cost
with `total_usd == Σ parts` enforced), signals (outbound events + inbound commands), discriminated
unions + TypeAdapters, Scalar-grade authoring (titles / attribute-docstrings / examples / discriminator).

## Planned changes
- `src/url4_cloud_protocol/{envelope,taxonomy,signals,unions,__init__}.py`
- `tests/unit/test_protocol.py`

## Test plan
- `total_usd != Σ parts` → ValidationError; valid breakdown ok; discriminated-union parse-by-`type`;
  Decimal → string on the wire; Span carries tokens, never cost.

## Acceptance
- Gates green; models importable; the cost invariant is unconstructible when violated.

## Outcome
- **Actual files:** as planned (envelope/taxonomy/signals/unions/__init__ + test_protocol).
- **Commits:** see the OME-515 commit on `OME-513-url4-cloud`.
- **Gates:** `run_gates.py url4-cloud` ALL GREEN (ruff · format · pyright · pytest+cov, 5 new tests).
- **Deviations:** ledger authored at close (code preceded the ledger this cycle — a slip; corrected
  for the remaining units); `subtree == self + Σ children` is a cross-frame invariant validated in
  the mock/e2e (OME-524), not a single-model validator.
