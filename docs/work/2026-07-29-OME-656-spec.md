---
ticket: OME-656
stack: repo
status: in_progress
started: 2026-07-29
finished:
---

# OME-656 — Studio UI revision: author the epic spec

## Intent

Author the epic-level design spec (`docs/spec/`) for the ScreamingFace Studio UI revision, per
CLAUDE.md spec-before-plan-before-code. Captures the composer redesign, per-model params, dual
url4 recipes, run-progress detail, fusions onboarding, models-page layout, and the copy-only
ensemble→fusion rename — the design contract the seven implementation sub-issues build against.

## Planned changes

- `docs/spec/2026-07-29-studio-ui-revision-spec.md`

## Test plan

- N/A — design artifact. Per-unit RED-first test plans live in each sub-issue's own ledger at
  code start. (No formal sdlc-* stack mapping — OME-657 was canceled — so per-unit gates default
  to `npm run lint`, `tsc --noEmit`, and Testing-Library component tests.)

## Acceptance

- Spec covers all 7 implementation sub-issues and pins (or explicitly defers, with the reason)
  the url4 recipe grammar; owner-reviewed.

## Outcome (fill at the end — required before COMMIT)

- **Actual files:** <vs planned>
- **Commits:** <sha — message>
- **Gates:** N/A (docs)
- **Deviations:** <none | list>
