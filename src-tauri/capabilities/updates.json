{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Permissions for the desktop updates window",
  "windows": ["updates"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close"
  ]
}
